export const metadata = {
  title: "진행 과정 | AURA",
  description: "AURA의 사용자 중심 디자인 띵킹 프로세스 — 필요 발견부터 최종 발표까지.",
};

import ProjectsClient from "@/components/ProjectsClient";

export default function ProjectsPage() {
  return <ProjectsClient />;
}
